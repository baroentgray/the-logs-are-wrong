# TLAW_DISPOSAL_LEVER_01 — Claude Sync Note

The approved binary for this package is expected as:

`TLAW_DISPOSAL_LEVER_01_REFERENCE_R01.png`

If the owner places a browser-downloaded copy into `prototype/art_pipeline/_incoming/`, identify it by the embedded marker:

`TLAW_DISPOSAL_LEVER_01 · REFERENCE · R01`

Then:

1. verify the marker and revision against `provenance.md`;
2. rename/copy it to the canonical filename above;
3. place it in `prototype/art_pipeline/TLAW_DISPOSAL_LEVER_01/`;
4. verify the file exists and is the approved `REFERENCE · R01`;
5. commit and push the binary;
6. update `provenance.md` status from `APPROVED_VISUALS_PENDING_BINARY_UPLOAD` to `APPROVED_READY` only after verification;
7. only then begin the Blender build described in `blender_handoff.md`.

`MAP` is explicitly `N/A`. Do not wait for or invent a map.
